dependencies:
  override:
    - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - zip -r bundle.zip /home/ubuntu/project-backend
      - aws s3 bundle.zip s3://playsparkbucket --delete